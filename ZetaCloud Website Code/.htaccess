# mod_rewrite starts here 

# RewriteEngine on

# does not apply to existing directories, meaning that if the folder exists on the server then don't change anything and don't run the Rule!

# RewriteCond %{REQUEST_FILENAME} !-d (Disabled)

# Check for file in directory with .html extension

# RewriteCond %{REQUEST_FILENAME}\.html -f (Disabled)

# Here we actually show the page that has the .html extension

# RewriteRule ^(.*)$ $1.html [NC,L] (Disabled)



# New Rule

# RewriteCond %{REQUEST_FILENAME} !-d (Disabled)
# RewriteCond %{REQUEST_FILENAME}\.html -f (Disabled)
# RewriteRule ^(.*)$ $1.html [NC,L] (Disabled)

# Disabled till I can find a way to run this server side. This exact code works without issues localhost side but server side doesnt load the pages. 